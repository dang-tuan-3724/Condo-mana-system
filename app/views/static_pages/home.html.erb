<div class="flex flex-col gap-12 items-center justify-center h-screen">
    <h1 class="font-bold text-4xl">Welcome to CMS</h1>
    <h2>This is home page for the CMS</h2>

    <% if !user_signed_in? %>
        <p class="border border-blue-500 rounded px-4 py-2"><%= link_to "Sign up now!", new_user_registration_path %></p>
    <% end %>
    
</div>
