<header style="background-color: rgba(63, 200, 254, 0.51); color: black; padding: 20px; position: fixed; top: 0; left: 0; right: 0; z-index: 50; border-radius: 50px; margin: 10px 20px 0px 20px; backdrop-filter: blur(5px); -webkit-backdrop-filter: blur(12px);">
  <div style="display: flex; gap: 20px; justify-content: center; align-items: center; text-black">

    <%# these roles are for everyone %>
    <%= link_to 'Home', root_path, style: 'color: black; text-decoration: none;' %>
    <%= link_to 'Facilities', facilities_path, style: 'color: black; text-decoration: none;' %>
    <%= link_to '[au] Member', users_path, style: 'color: black; text-decoration: none;' %>
    <%= link_to '[r] Notifications', "#", style: 'color: black; text-decoration: none;' %>
    <%= link_to '[au] Condo', "#", style: 'color: black; text-decoration: none;' %>
    <%= link_to '[au] Units', units_path, style: 'color: black; text-decoration: none;' %>

    <%# these roles are just for admin %>
    <% if current_user && %w[super_admin admin].include?(current_user.role) %>
          <%= link_to '[a] Bookings', "#", style: 'color: black; text-decoration: none;' %>
    <% end %>
    
    <%# The following buttons will be displayed depending on your login status  %>
    <% if user_signed_in? %>
      <%= link_to '[r]Profile', user_path(current_user), style: 'color: black; text-decoration: none;' %>
    <% else %>
        <%= link_to '[r]Login', new_user_session_path, style: 'color: black; text-decoration: none;' %>
    <% end %>  

    <%# Display current loggin role %>
    <p class="">[r]<%= current_user.role if current_user  %></p> 
    </div>


  </header>
